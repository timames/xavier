{
  "name": "xavier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create xavier",
    "db:migrate": "wrangler d1 execute xavier --file=schema.sql",
    "db:migrate:prod": "wrangler d1 execute xavier --file=schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
